import React from 'react';
import { Provider } from 'react-native-maps';
export type GradientPolylinesFunctionalProps = {
    provider: Provider;
};
declare const GradientPolylines: (props: GradientPolylinesFunctionalProps) => React.JSX.Element;
export default GradientPolylines;
